<!-- newinfo.ejs -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Information</title>
    <style>
      /* General styles for body and headings */
      body {
        margin: 0;
        min-height: 100vh;
        display: grid;
        place-content: center;
        background: repeating-linear-gradient(
          -45deg,
          #fff 0 20px,
          #f9f9f9 0 40px
        );
        background: rgb(52, 2, 43);
        background: linear-gradient(
          90deg,
          rgba(52, 2, 43, 1) 3%,
          rgba(2, 0, 36, 1) 52%,
          rgba(1, 50, 59, 1) 85%
        );
        font-family: "Georgia", serif;
        color: rgba(221, 115, 115, 0.53);
      }

      /* Additional styles for specific elements */
      h1 {
        text-align: center;
        font-size: 2em;
        margin-bottom: 20px;
      }

      form {
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      input,
      textarea {
        padding: 10px;
        margin-bottom: 15px;
        width: 100%;
        box-sizing: border-box;
      }

      button {
        background-color: #ffeeee;
        color: #333;
        padding: 10px 15px;
        margin-top: 10px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #dd9999;
      }
      button {
        width: 180px;
        font-size: 16px;
        font-weight: 600;
        cursor: pointer;
        margin-top: 5px;
        height: 45px;
        text-align: center;
        border: none;
        background-size: 300% 100%;
        border-radius: 50px;
        transition: all 0.4s ease-in-out;
        background-image: linear-gradient(
          to right,
          #ed6ea0,
          #ec8c69,
          #f7186a,
          #fbb03b
        );
        box-shadow: 0 4px 15px 0 rgba(236, 116, 149, 0.75);
      }
      .container {
        text-align: center;
        background-color: rgba(255, 255, 255, 0.1);
        /* padding: 20px; */
        border-radius: 10px;
        /* margin-top: 20px;  */
        width: 400px;
        height: 200px;
      }
      button:hover {
        background-position: 100% 0;
        transition: all 0.4s ease-in-out;
      }

      button:focus {
        outline: none;
      }
      input {
        background-color: rgba(255, 255, 255, 0.1);
        /* Adjust the opacity to your liking */
        color: #113a62;
        border: none;
        padding: 10px;
        margin-bottom: 15px;
        border-radius: 5px;
        width: 80%;
        box-sizing: border-box;
        outline: none;
        background: linear-gradient(
          to right,
          rgba(255, 153, 102, 0.8),
          rgba(255, 94, 98, 0.8)
        );
      }
      input::placeholder {
        color: #113a62;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Add New Information</h1>
      <form action="/saveinfo" method="post">
        <!-- Add input fields for new information -->
        <input
          type="hidden"
          name="token"
          id="token"
          value="<%= user.token %>"
        />
        <!-- <input type="hidden" name="user" id="user" value="<%= //user %>" /> here when we just pass user we get [object object] as in the req.body but i want complete info do following:This code uses JSON.stringify() to convert the user object to a JSON string. When you submit the form, this string will be sent to the server.-->
        <input
          type="hidden"
          name="user"
          id="user"
          value="<%= JSON.stringify(user) %>"
        />
        <input
          type="text"
          name="bio"
          id="bio"
          placeholder="Write something about you"
        />
        <button type="submit">Save Information</button>
      </form>
    </div>
  </body>
</html>
