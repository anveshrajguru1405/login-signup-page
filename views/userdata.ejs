<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>user</title>
    <style>
      /* General styles for body and headings */
      body {
        margin: 0;
        min-height: 100vh;
        display: grid;
        place-content: center;
        background: repeating-linear-gradient(
          -45deg,
          #fff 0 20px,
          #f9f9f9 0 40px
        );
        background: rgb(52, 2, 43);
        background: linear-gradient(
          90deg,
          rgba(52, 2, 43, 1) 3%,
          rgba(2, 0, 36, 1) 52%,
          rgba(1, 50, 59, 1) 85%
        );
        font-family: "Georgia", serif;
        color: rgb(217, 82, 19);
      }
      .container {
        background-color: rgba(255, 255, 255, 0.1);
        padding: 20px;
        border-radius: 10px;
      }
      h1,
      h2,
      h3,
      p {
        text-align: center;
        margin-bottom: 10px;
      }

      h1 {
        font-size: 2em;
      }

      h2 {
        font-size: 1.5em;
      }

      h3 {
        font-size: 1.2em;
      }

      p {
        font-size: 1em;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Hello <%= data.values[0] %></h1>
      <h2>Glad to see you here</h2>
      <p>Your token id is: <%= data.values[5]%></p>
      <h3>Roll No: <%= data.values[1] %></h3>
      <h3>Room No: <%= data.values[2] %></h3>
      <h3>Email: <%= data.values[3] %></h3>
      <h3>Hall of Residence <%= data.values[6] %></h3>
      <h3>Passing Year <%= data.values[7] %></h3>
    </div>
  </body>
</html>
