<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="icon" href="data:" />
    <link rel="stylesheet" href="../home.css" />
    <link rel="stylesheet" href="../login.css" />
  </head>
  <body>
    <form id="login" autocomplete="off" method="post" action="/login">
      <!-- Change the method to "post" and set the action to "/login" -->
      <div id="signin-form" class="box shadow">
        <h2>Login</h2>
        <label for="signin-email">Email:</label>
        <input
          type="email"
          id="signin-email"
          name="email"
          placeholder="Email"
          required
        />
        <label for="signin-password">Password:</label>
        <input
          type="password"
          id="signin-password"
          name="password"
          placeholder="Password"
          required
        />
        <button type="submit">
          <!-- Change the anchor tag to a button type="submit" -->
          <span class="bn39span">Login</span>
        </button>
        <p>
          <a href="/forgot-password">Forgot Password?</a>
          <!-- Link to the "Forgot Password" page -->
        </p>
      </div>
    </form>
  </body>
</html>
